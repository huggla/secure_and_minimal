#!/bin/sh
set -e +a +m +s +i -f

. start.stage1.functions

if [ isSomethingToDo ]
then
   if [ isFirstRun ]
   then
      updateSudoersConf
      writeRuntimeEnvironment
   fi
   startStage2
else
   echo "Nothing to do!"
fi
