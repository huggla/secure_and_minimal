startStage2(){
   local isFirstRun=$1
   readEnvironmentVars
   if [ "$isFirstRun" == "true" ]
   then
      createLinuxUser
      updateSudoersConf
      fixHosts
      makeAllFromList "$(allVars noquote)"
      fixPermissionsForUsrLocalBin
   fi
   tryRunStage 3
   allVars quote > /environment/vars
   if [ "$VAR_FINAL_CMD_AS_ROOT" == "yes" ]
   then
      execCmd /start/final "$VAR_FINAL_COMMAND"
   else
      execCmdAsLinuxUser /start/final "$VAR_FINAL_COMMAND"
   fi
}
