execFinalCmd(){
   if [ "$isFirstRun" == "true" ]
   then
      local command="$(eval "echo $1")"
      {
         echo "$command &"
         echo "wait"
      } >> /stop/stage1
   fi
   exec /usr/bin/env -i /stop/stage1
}
