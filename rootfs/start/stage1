#!/bin/sh
set -e +a +m +s +i -f
. /start/functions/writeEnvironmentVars

if [ ! -e "/environment/vars" ]
then
   writeEnvironmentVars
   isFirstRun="true"
fi
/usr/bin/env -i /stop/stage1 &
exec /usr/bin/env -i /start/stage2 $isFirstRun
