stop(){
   local -
   set -e
   tryRunStage 2
   /usr/local/bin/killpid $PID $sig
   if [ "$PID" -eq 1 ]
   then
      echo "Container was stopped! Signal $sig"
   else
      echo "Process was stopped! Signal $sig"
   fi
}
