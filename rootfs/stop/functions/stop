stop(){
   local -
   set -e
   tryRunStage 2
   /usr/local/bin/killpid $PID $sig
   if [ "$PID" == "1" ]
   then
      echo "Container was stopped!"
   else
      echo "Process was stopped!"
   fi
}
