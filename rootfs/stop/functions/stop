stop(){
   local -
   set -e
   tryRunStage 2
   /usr/local/bin/killpid $PID $1
   if [ "$PID" -eq 1 ]
   then
      echo "Container was stopped! Signal $1"
      exit 0
   else
      echo "Process was stopped! Signal $1"
      exit 0
   fi
}
