execCmd(){
   exec /usr/bin/env -i "$(eval "echo $1")"
}

execCmdAsLinuxUser(){
   if [ -n "$1" ]
   then
      execCmd "/usr/local/bin/sudo -u $VAR_LINUX_USER $(eval "echo $1")"
   else
      echo 'execCmdAsLinuxUser: Nothing to do!'
   fi
}
