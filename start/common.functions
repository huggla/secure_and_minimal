# Set in parent script:
# -------------------------------------------
# set -e +a +m +s +i -f

# Set in Dockerfile or at runtime:
# -------------------------------------------
# VAR_LINUX_USER


isFirstrun(){
   if [ -f "/environment/firstrun" ] && [ -z "$firstrun" ]
   then
      firstrun="true"
   else
      firstrun="false"
   fi
   echo "$firstrun"
}
