# Set in parent script:
# -------------------------------------------
# set -e +a +m +s +i -f

# Set in Dockerfile or at runtime:
# -------------------------------------------
# VAR_LINUX_USER


isFirstRun(){
   if [ -f "/environment/runtime" ]
   then
      echo "true"
   else
      echo "false"
   fi
}
