# Set in parent script:
# -------------------------------------------
# set -e +a +m +s +i -f


isFirstrun(){
   if [ -f "/environment/firstrun" ]
   then
      firstrun="true"
   elif [ "$firstrun" != "true" ]
   then
      firstrun="false"
   fi
   echo "$firstrun"
}

executeCommand(){
   exec /usr/bin/env -i $1
}
