#!/bin/sh
set -e +a +m +s +i -f

. /start/common.functions
. /start/stage1.functions

if [ "$(isSomethingToDo)" == "true" ]
then
   if [ "$(isFirstrun)" == "true" ]
   then
      createLinuxUser
      updateSudoersConf
      writeFirstrunEnvironment
   fi
   startStage2
else
   echo "Nothing to do!"
fi
