#!/bin/sh
set -e +a +m +s +i -f

. /start/common.functions
. /start/stage1.functions

if [ "$(isSomethingToDo)" == "true" ]
then
   if [ -f "/environment/firstrun" ]
   then
      firstrun="true"
      createLinuxUser
      updateSudoersConf
      writeFirstrunEnvironment
   else
      firstrun="false"
   fi
   executeCommand "firstrun=$firstrun /start/stage2"
else
   echo "Nothing to do!"
fi
